# Production requirements for Vercel deployment
# Optimized for serverless functions (under 250MB limit)

# Core Flask dependencies
flask==2.3.3
werkzeug==2.3.7

# Database and API
supabase==1.0.4
requests==2.31.0

# Authentication
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Configuration
python-dotenv==1.0.0

# QR Code generation (lightweight)
qrcode==7.4.2

# Note: Removed opencv-python and pyzbar due to size constraints
# QR scanning is handled client-side with html5-qrcode.min.js
